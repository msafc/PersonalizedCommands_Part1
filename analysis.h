#define _CRT_SECURE_NO_WARNINGS 
char* findInFile(const char* path, const char* word);

long extractValue(char* line, const char* delimiter);

void showFile(const char* file);

int leecad(char* cad, int n);